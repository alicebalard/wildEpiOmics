## Cards for each study, entered through "issues"

Optional field `condition` holds comma-separated keyword(s) describing the
condition(s) studied (e.g. `captivity, stress`). Shown on each card and
filterable from the site sidebar.
