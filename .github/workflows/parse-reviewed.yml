name: Parse Reviewed Entry

on:
  issues:
    types: [labeled]

jobs:
  process:
    if: github.event.label.name == 'reviewed'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install js-yaml
        run: npm install js-yaml

      - name: Parse issue → data/*.yaml
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const yaml = require('js-yaml');
            const issue = context.payload.issue;
            const issueNumber = issue.number;
            
            // Get issue body
            const { data: issueData } = await github.rest.issues.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber
            });
            const body = issueData.body || '';

            console.log('Processing issue:', issueNumber);

            // Parse form fields (ALWAYS - simplified)
            const doiMatch = body.match(/doi[:\s]*`?([^`\n]+)/i);
	    const taxMatch = body.match(/.*Taxon.*ID[:\s]*`?(\d+)/i) || body.match(/taxid[:\s]*`?(\d+)/i);
	    const tissueMatch = body.match(/tissue[:\s]*`?([^`\n]+)/i);
            const methodMatch = body.match(/method[:\s]*`?([^`\n]+)/i);
            const individualsMatch = body.match(/number of sequenced individuals[:\s]*`?(\d+)/i);
            const notesMatch = body.match(/notes[:\s]*([\s\S]*?)(?=\n###|\n$)/i);
            
            const taxid = taxMatch ? taxMatch[1] : 'unknown';  // ✅ MOVED UP
            const entryYear = (body.match(/\b(19|20)\d{2}\b/) || ['2024'])[0];
            const doi = doiMatch ? doiMatch[1] : 'no-doi';
            const tissue = tissueMatch ? tissueMatch[1].trim() : '';
            const method = methodMatch ? methodMatch[1].trim() : '';
            const individuals = individualsMatch ? individualsMatch[1] : '';
            const notes = notesMatch ? notesMatch[1].trim() : '';

            const taxonInfo = {
              '69293': { order: 'Gasterosteiformes', class: 'Actinopterygii' }
            };

            const studyData = {
              taxid,
              doi,
              year: entryYear,
              method,
              tissue,
              individuals: parseInt(individuals) || 0,
              title: notes.split('\n')[0] || 'Untitled study',
              authors: 'Unknown authors',
              url: doi.startsWith('10.') ? `https://doi.org/${doi}` : doi,
              order: taxonInfo[taxid]?.order || 'Unknown',
              class: taxonInfo[taxid]?.class || 'Unknown'
            };
            
            const yamlText = yaml.dump(studyData, { indent: 2 });

            if (!yamlText || !taxid || taxid === 'unknown') {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body: '❌ Missing `taxid`. Please include ### NCBI Taxon ID.'
              });
              return;
            }

            // Generate filename - taxid NOW available ✅
            const fileYear = entryYear;
            const base = `data/${taxid}_${fileYear}`.replace(/[^a-zA-Z0-9_/-]/g, '');
            let filename = `${base}.yaml`;
            if (fs.existsSync(filename)) filename = `${base}_${issueNumber}.yaml`;

            console.log('Writing:', filename);
            fs.writeFileSync(filename, yamlText);

            // Git operations
            await exec.getExecOutput('git', ['config', 'user.name', 'github-actions[bot]']);
            await exec.getExecOutput('git', ['config', 'user.email', 'github-actions[bot]@users.noreply.github.com']);
            await exec.getExecOutput('git', ['add', filename]);
            
            const commitResult = await exec.getExecOutput('git', ['commit', '-m', `Add study #${issueNumber}`]);
            if (commitResult.exitCode === 0) {
              await exec.getExecOutput('git', ['push', 'origin', 'main']);
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body: `✅ **Added!** \`${filename}\` → [View](https://github.com/${context.repo.owner}/${context.repo.repo}/blob/main/${filename})`
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body: `ℹ️ File \`${filename}\` already exists`
              });
            }
            
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              state: 'closed'
            });
